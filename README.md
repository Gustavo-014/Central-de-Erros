# Central de Erros

Ferramenta para automatizar a geração de mensagens de erro para clientes da Brobot.

Tecnologias utilizadas.

Como executar:

python -m venv .venv

ativar ambiente

pip install -r requirements.txt

python -m streamlit run app.py
